<h1>Page Profil</h1>

<p>Bienvenue sur la page d'ajout d'un avatar pour le User portant lid : <%=@users.id%></p>
<p>Bienvenue sur la page d'ajout d'un avatar pour le User portant lid : <%=@users.city%></p>
<h3>Avatar actuel</h3>
<%if @users.avatar.attached?%>
  <%= image_tag @user.avatar, alt: 'avatar' %>
<%else%>
  <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
<%end%>


<h3>Create or update your avatar</h3>
<%= form_tag user_avatars_path(@users), multipart: true do %>
  <%= file_field_tag :avatar %>
  <%= submit_tag "mettre à jour" %>
  <%=@users.last_name%>
<% end %>